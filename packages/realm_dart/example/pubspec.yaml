name: myapp
description: A simple command-line application using Realm Dart SDK.

publish_to: none

environment:
  sdk: ^3.6.0
resolution: workspace

dependencies:
  realm_dart:
    path: ../

dev_dependencies:
  lints: ^6.0.0
